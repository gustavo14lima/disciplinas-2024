// Array para as cores red e blue
    const colors1 = ['red','blue']
// Array para as cores green e yellow
    const colors2 = ['green','yellow']
// Array para juntar as cores
    const combinedColors = colors1.concat(colors2,'purple')

    console.log(combinedColors) // A saida do console deve ser: ['red','blue','green',yello','purple']